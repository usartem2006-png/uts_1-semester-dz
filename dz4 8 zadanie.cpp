//8. Вычислите значение выражения:a.(𝑎 +4𝑏)(𝑎 −3𝑏)+𝑎^2. При a=2 и b=3 - ответ:  -94
#include <iostream>//ввод-вывод
using namespace std;// Использование стандартного пространства имен

int main() {//начало функции main
    int a = 2;//объявление переменной
    int b = 3;//объявление переменной

    // Вычисляем выражение: (a + 4b)(a - 3b) + a²
    int result = (a + 4*b) * (a - 3*b) + a*a;//вычисление результата

    cout << "При a = " << a << " и b = " << b << endl;//вывод значений a и b
    cout << "Выражение: (a + 4b)(a - 3b) + a²" << endl;//вывод выражения
    cout << "Результат: " << result << endl;//вывод результата

    return 0;//завершение функции main
}

